# 방법론 설명 문서

**대회**: 제5회 대학 연합 아주 소중한 딥러닝 챌린지 2026 — LLM 수학 문제 해결
**참가자**: gamaius
**베이스 모델**: `Qwen/Qwen2.5-3B-Instruct`
**작성일**: 2026-08-25 *(초안 — 최종 점수 확정 후 갱신)*

> 규칙 8.2a의 다섯 항목 중 「방법론 설명 문서」에 해당합니다.
> 나머지 항목(코드·가중치·데이터셋 목록·환경 설명)은 §7~§8에 정리했습니다.

---

## 1. 요약

정수 답 수학 문제를 푸는 3B 모델을 만드는 과제에서, **성능을 묶고 있던 것은 학습 기법이
아니라 학습 데이터였다**는 진단에서 출발했다. 두 번의 데이터 문제를 실측으로 찾아 고쳤다.

1. **1차 진단 — 답 포맷 결손**: 주최측 제공 풀이의 30.4%가 `\boxed{}` 없이 끝나,
   모델이 "최종 답을 명시하지 않는" 습관을 학습하고 있었다.
2. **2차 진단 — 정제 필터의 선택 편향**: 그 정제 과정에서 쓴 필터가 *정답의 자릿수*와
   상관되어, 쉬운 문항을 집중적으로 탈락시키고 있었다. 학습셋이 어려운 쪽으로 치우쳐 있었다.

두 번째 문제는 **거절 샘플링 자기학습(Rejection Sampling Fine-Tuning, RFT)** 으로 해결했다.
현재 모델이 주최측 문항 18,319개를 직접 풀게 하고 정답과 일치한 풀이만 남겨,
편향 없는 in-distribution 학습 데이터를 재구축했다.

추론은 Self-Consistency(다수결)를 쓰되, **합의율(agreement rate)이 정확도의 강한 예측자**
라는 실측에 기반해 표 배분을 설계했다.

| 단계 | 로컬 검증 | 리더보드(Public) |
|---|---|---|
| 순정 Qwen2.5-3B-Instruct | 0.5380 | — |
| 1차 정제 + SFT (v4) | 0.7280 | 0.74969 |
| 실행 간 표 합산(pooling) | — | **0.75812** |
| 2차(RFT) 재학습 | 「TBD」 | 「TBD」 |

---

## 2. 문제 진단

### 2-1. 파인튜닝이 오히려 성능을 낮추고 있었다

같은 조건에서 측정했을 때:

| 모델 | 정확도 | 조건 |
|---|---|---|
| 순정 Qwen2.5-3B-Instruct | 0.5380 | 처음 보는 문항 |
| 기존 파인튜닝(DPO) 모델 | 0.5340 | **학습 때 본 문항인데도** |

학습 때 본 문항에서조차 베이스보다 못하다는 것은 학습이 모델을 개선하지 못했다는 뜻이다.
원인을 데이터에서 찾았다.

### 2-2. 학습 데이터 실측 (`deep_chal_math_master_train_v3.csv`, 19,074문항)

| 문제 | 규모 | 영향 |
|---|---|---|
| `\boxed{}` 없이 종료 | `competition_gemini_cot` 16,359개 중 **30.4%** | 답 포맷 미학습 → 후처리가 중간 계산값을 답으로 추출 |
| 정답 10회 이상 재진술 | **35%** (중앙값 7회, 최대 129회) | "앵무새 증후군"의 실제 원인 |
| 평가셋 유출 | 리더보드 831문항 중 **255개**가 학습셋에 존재 | 일반화 성능 과대평가 + 규칙 리스크 |

한편 `\boxed{}`가 있는 샘플은 그 값의 정확도가 **99.84%**였다.
즉 **답은 멀쩡했고 형식과 장황함만 문제**였다. `gsm8k_real_cot` 2,715개는 100% 정상이었다.

> **설계 판단**: 기존 코드는 `repetition_penalty=1.15`로 반복을 억제하고 있었으나,
> 이는 증상 억제일 뿐 아니라 **수학적으로 유해**하다. 풀이는 같은 숫자·변수를 반복해서
> 써야 하는데, 페널티가 두 번째 등장 숫자의 확률을 깎아 계산 자체를 훼손한다.
> 원인을 데이터에서 고치고 추론에서는 **1.0으로 되돌렸다.**

### 2-3. 2차 진단 — 정제 필터의 선택 편향 *(본 대회에서 얻은 핵심 발견)*

1차 정제는 "정답을 8회 초과 재진술한 풀이 제외" 규칙을 썼다. 그런데 재진술 횟수는
**정답의 자릿수와 강하게 교란**된다. 답이 `3`이면 풀이 중간 계산에 `3`이 계속 등장해
카운트에 걸리고, 답이 `1247`이면 걸리지 않는다.

RFT 생성 결과(정답 풀이 50,875개)로 실측한 탈락률:

| 정답 크기 | 재진술 중앙값 | 임계 8 탈락률 |
|---|---|---|
| 한 자리 | 3.0 | **15.9%** |
| 두 자리 | 2.0 | 2.2% |
| 세 자리 | 2.0 | 1.2% |
| 네 자리 이상 | 2.0 | **1.1%** |

**14배 차이**다. 그리고 작은 답을 가진 문항이 대체로 더 쉽다. 결과적으로 이 필터는
"쉬운 문항을 골라서 버리는" 장치로 작동했다.

실제 결과를 학습 포함 여부로 갈라 측정하면:

| `competition_gemini_cot` | 문항 수 | 정확도 |
|---|---|---|
| 정제를 통과해 **학습에 쓰인** 것 | 6,468 | **0.5158** |
| 정제에서 **걸러진** 것 | 9,458 | **0.7783** |

정답 중앙값도 60 대 17, 한 자리 답 비율은 15.2% 대 39.3%로 갈렸다.
학습셋이 어려운 쪽으로 심하게 치우쳐 있었고, 주최측 데이터의 60%가 버려져 있었다.

> 이 발견은 "학습 때 본 문항 vs 안 본 문항"의 정확도를 비교하다 **격차가 음수(−0.157)** 로
> 나온 것을 이상하게 여겨 추적한 결과다. 학습은 정의상 본 문항을 더 잘 맞히게 하므로,
> 음수 격차는 두 집단의 난이도가 애초에 달랐다는 신호였다.

---

## 3. 데이터 구축

### 3-1. 1차 정제 (`01_clean_data.py`)

`deep_chal_math_master_train_v3.csv` → `deep_chal_math_clean_v4.csv` (8,269개)

필터: `\boxed{}` 정확히 1개 · 그 값이 정답과 일치 · 정답 재진술 8회 미만 ·
풀이 3,000자 이하 · 평가셋 유출 제거.

부산물로 **`local_val_500.csv`** (학습에 쓰지 않은 500문항)를 분리했다.
제출 없이 성능을 측정하기 위한 검증셋이며, 리더보드와 분포가 거의 같다
(질문 길이 235자 대 232자, LaTeX 포함률 44% 대 42%).

### 3-2. 외부 데이터 추가 (`04_add_external_data.py`)

3B 모델의 수학 추론에는 8.2k가 부족하다고 판단해 40,269개로 확장했다.
모든 샘플에 대해 정수 정답 확인, `\boxed{}` 검증, 평가셋·검증셋 유출 제거를 통과시켰다.

### 3-3. RFT — 거절 샘플링 자기학습

**동기.** §2-3의 편향을 근본적으로 없애려면, *풀이의 품질로 문항을 선별하는 방식* 자체를
버려야 한다. 문항은 전부 쓰되 **풀이를 새로 생성**하면 된다.

**절차**

1. **대상 풀 구성** (`07_make_rft_pool.py`) — 주최측 19,074문항에서 리더보드·검증셋과
   중복되는 755개를 제거 → **18,319문항**. 정답은 전부 보유하고 있다.
2. **생성** (`02_infer_vllm.py --dump-gens`) — 현행 최고 모델(v4)로 문항당 8회 샘플링.
   `temperature 0.7`, `top_p 0.95`, `max_tokens 1024`, `repetition_penalty 1.0`.
3. **검증** — 추출한 답이 정답과 **일치하는 풀이만** 채택 (거절 샘플링).
4. **필터** (`06_build_rft_data.py`) — `\boxed{}` 정확히 1개 · 3,000자 이하 ·
   재진술 20회 이하 · 문장 뼈대(숫자를 마스킹한 형태) 기준 중복 제거 ·
   **문항당 최대 2개**.

**문항당 상한이 난이도 균형 장치다.** 8회 모두 맞히는 쉬운 문항은 풀이가 많이 나오고
1회만 맞히는 어려운 문항은 하나뿐이므로, 상한이 없으면 학습셋이 쉬운 문항으로 덮인다.

**재진술 임계를 8에서 20으로 올렸다.** §2-3에서 밝힌 편향 때문이며, 임계 20에서는
한 자리 답 탈락률이 15.9% → 4.6%로 떨어지면서도 진짜 앵무새(최대 129회)는 여전히 걸러진다.
RFT 풀이는 현행 모델이 생성한 것이라 재진술 중앙값이 2~3회로 이미 깨끗하다.

**결과**

| 항목 | 값 |
|---|---|
| 대상 문항 | 18,319 |
| 한 번 이상 정답 도달 (pass@8) | 14,941 (**81.6%**) |
| 최종 채택 문항 | 14,105 |
| 학습 샘플 | **26,924** (문항당 평균 1.91개) |
| 미해결 문항 | 3,378 → 2차 생성 대상 |

**편향 제거 확인** — 정답 크기 분포가 원본 풀과 거의 일치한다:

| 데이터셋 | 문항 | 정답 중앙값 | 한 자리 답 | 세 자리+ 답 |
|---|---|---|---|---|
| `rft_pool` (편향 없는 기준) | 18,319 | 31 | 27.7% | 31.1% |
| 기존 학습셋(주최측분) | 5,919 | 78 | 13.3% | 46.1% |
| **RFT 학습셋** | **14,105** | **30** | **28.2%** | **28.6%** |

주최측 문항 커버리지가 5,919 → 14,105로 **2.4배** 증가했다.

### 3-4. 라벨 불량 문항 제외 (`10_find_bad_labels.py`)

주최측이 커뮤니티 제보를 받아 **"의도된 것이 아니라 원본 소스 단계에서 유입된 오류"** 로
인정하고, 참가자가 각자 판단해 학습에서 제외하도록 안내한 문항들이 있다 (2026-08-18 운영팀 답변).

**라벨이 틀린 문항에서는 거절 샘플링이 거꾸로 작동한다.** 모델이 올바르게 풀면 정답 대조에
실패해 버려지고, 틀린 답이 우연히 틀린 라벨과 일치하면 그 풀이가 학습 데이터로 들어간다.
즉 **모델에게 틀린 답을 가르치는** 방향의 오염이다.

제보 목록(mislabel 442 + ill-posed 623 + 개별 제보 14)을 반영하되, 여기에 그치지 않고
**모델 합의를 신호로 미제보 오류를 추가 탐지**했다.

**판별식**: `정답 0개 & 합의율 높음` — 모델이 확신을 갖고 일관되게 X를 답했는데 라벨은 Y.
못 푼 것이 아니라 라벨이 틀렸을 가능성이 높다.

**판별식 검증** (18,319문항 실측):

| 구분 | 0정답 비율 | 평균 합의율 |
|---|---|---|
| 제보 mislabel | **66.3%** | 0.426 |
| 제보 ill-posed | **63.8%** | 0.336 |
| 나머지 | 15.8% | 0.602 |

0정답 문항 중 제보 오류일 확률은 합의율에 따라 단조 증가했다
(합의율 <0.3 에서 14.1% → 0.5~0.7 에서 35.1% → ≥0.7 에서 **48.6%**).

가장 강한 증거는 상호 검증이다. **제보 mislabel 442건 중 64.3%에서, 우리 모델의 최빈답이
제보자가 수작업으로 계산한 값과 정확히 일치했다.** 완전히 독립적인 두 출처가 같은 결론에
도달한 것으로, 제보 목록의 신뢰성과 판별식의 타당성을 동시에 뒷받침한다.

**결과**: 제보 1,067건 + 자동 탐지 89건 → `rft_pool` 내 **1,087문항** 제외.
학습 샘플에서 **294문항분(367샘플)** 이 빠졌다 — 틀린 라벨과 일치해 "정답"으로 채택될 뻔한 풀이들이다.

**성능 천장 재계산**: 8회 시도에서 한 번도 정답에 도달하지 못한 3,378문항 중
**736개(21.8%)가 라벨 불량으로 설명된다.** 실제로 모델이 못 푸는 문항은 2,642개(14.4%)이며,
보정 pass@8은 **0.816 → 0.856**이다.

주최측이 최종 평가 데이터에서는 동일한 유형의 오류를 배제하겠다고 밝혔으므로,
**test 셋은 train 셋보다 깨끗하다.** train 풀에서 측정한 수치들은 test 성능을 과소평가한다.

### 3-5. 최종 학습 데이터 (`09_build_train_mix.py`)

| 출처 | 샘플 | 비중 | 역할 |
|---|---|---|---|
| `rft_self` | 26,924 | 65.2% | in-distribution, on-policy, 정답 검증됨 |
| `external` | 12,000 | 29.1% | 다양성 보조 |
| `gsm8k_real_cot` | 2,350 | 5.6% | 사람이 쓴 골드 스탠다드 |
| **합계** | **41,274** | | 13.5M 토큰 |

기존 `competition_gemini_cot` 풀이는 **제외**했다. 같은 문항을 RFT가 편향 없이 덮기 때문이다.
외부 데이터 비중을 79% → 29.1%로 낮춘 것은, 대회 test와 분포가 다른 데이터가
학습셋의 대부분을 차지하던 구조를 바로잡기 위해서다.

---

## 4. 학습

### 4-1. 설정

| 항목 | 값 | 근거 |
|---|---|---|
| 방식 | QLoRA (4bit) + SFT | 단일 T4(16GB)에서 3B 학습 |
| LoRA rank / alpha | 32 / 64 | 수학 추론에 표현력이 더 필요 |
| target modules | q/k/v/o, gate/up/down | 전 선형층 |
| **learning rate** | **2e-4** | QLoRA 표준값. §4-2 참조 |
| scheduler | cosine, warmup 3% | |
| max_seq_length | 1,536 | 학습셋 토큰 p99 = 1,268. 잘림 0.03% |
| batch | 2 × grad accum 8 (실효 16) | T4 메모리 한계 |
| epochs | 1 | 같은 비용이면 데이터 다양성이 유리 |
| optimizer | adamw_8bit | |
| loss 마스킹 | response-only | 점검 결과 loss 대상 토큰 55.2% |

**학습 텍스트 형식** (추론 프롬프트와 반드시 동일):

```
<|im_start|>system
Please reason step by step, and put your final answer within \boxed{}.<|im_end|>
<|im_start|>user
{question}<|im_end|>
<|im_start|>assistant
{solution}<|im_end|>
```

### 4-2. 학습률 결정 과정

이전 학습은 `learning_rate=1e-5`를 썼다. QLoRA 표준값 2e-4의 **1/20**이며,
40k × 1 epoch(약 2,500 step)에서 스텝 수로 벌충되지도 않는 값이다.
낮춘 이유는 "Instruct 정렬 보호"였으나, §2에서 확인했듯 이전 학습이 망가진 원인은
학습률이 아니라 데이터였다. 원인이 아닌 변수를 과보정한 것으로 판단했다.

한 번뿐인 본 학습을 검증 없이 던지지 않기 위해, **10,000샘플 축소 실행**으로 먼저 확인했다
(구성 비율 유지, 625 step, 1시간 34분):

| 지표 | 결과 |
|---|---|
| train_loss | 0.370 → **0.258** (부드러운 하강, NaN·발산 없음) |
| grad_norm | 0.772 → 0.202 (안정적 감소) |
| 리더보드 점수 | 「TBD」 |

시작 loss가 0.37로 낮은 것은 학습 데이터의 66%가 모델 자신이 생성한 RFT 풀이이기 때문이다
(on-policy 데이터는 이미 높은 확률을 부여받는다). RFT의 학습 신호는 "새로운 것을 배워라"가
아니라 "맞혔던 방식 쪽으로 확률을 몰아라"에 가깝다.

---

## 5. 추론

### 5-1. Self-Consistency

규칙 6c가 허용하는 test-time 기법 중 다수결(Self-Consistency)을 사용한다.
vLLM의 `SamplingParams(n=K)`는 프롬프트 KV 캐시를 공유하므로 K배보다 훨씬 저렴하다.

`temperature 0.7`, `top_p 0.95`, **`repetition_penalty 1.0`** (§2-2 참조).
프롬프트에 "be concise" 류의 지시는 넣지 않는다 — CoT를 짧게 만들면 정확도가 떨어진다.

### 5-2. 정답 추출 (3단계 등급 폴백)

모델 출력에서 정수를 뽑는 후처리는 참가자 책임이며(규칙 7.2d), 잘못 설계하면
중간 계산값을 답으로 제출하게 된다. 등급을 매겨 처리한다:

| 등급 | 방식 |
|---|---|
| 2 | `\boxed{...}` |
| 1 | 꼬리 문장의 "answer is N" |
| 0 | 마지막 *줄*의 숫자 |
| −1 | 추출 실패 → **투표에서 제외** (0을 반환하지 않는다) |

**`\boxed{}`를 낸 샘플이 하나라도 있으면 그 표들끼리만 투표한다.** 등급이 낮은 추출은
신뢰도가 낮으므로 섞지 않는다.

LaTeX 처리: `\dfrac{10}{2}` → 5, `1,200` → 1200, 잘린 `\boxed{1234` → 1234,
거대수(>10¹⁵)는 무효 처리. 15개 케이스 단위 테스트를 통과한다.

### 5-3. 합의율은 정확도의 강한 예측자다

`local_val_500` 실측 (n=16):

| 합의율 | 문항 비중 | 정확도 |
|---|---|---|
| ≥ 0.7 | 43% | **97.7%** |
| 0.4 ~ 0.7 | 27% | 79.1% |
| < 0.4 | 31% | **32.7%** |

오답 136개 중 116개(85%)가 합의율 0.5 미만에서 나왔고, "확신하며 틀린" 경우는 4개뿐이었다.

리더보드 831문항에서 **동일 설정 두 실행을 비교**한 결과가 이를 뒷받침한다:

| 합의율 | 문항 수 | 두 실행 간 답 불일치 |
|---|---|---|
| ≥ 0.7 | 383 (46%) | **0개 (0.0%)** |
| 0.4 ~ 0.7 | 186 (22%) | 8개 (4.3%) |
| < 0.4 | 262 (32%) | **114개 (43.5%)** |

**전체 분산의 93%가 전체 문항의 32%에서 발생한다.** 나머지 68%는 표를 늘려도 변하지 않는다.

### 5-4. 실행 간 표 합산 (pooling)

위 관측의 직접적 귀결로, 동일 설정 실행 간 점수 표준편차가 σ ≈ 0.013이었다.
단발 실행은 사실상 추첨이므로, **여러 실행의 표를 합산**해 분산을 제거했다.

리더보드 실측: 단일 n=32 최고 0.74969 → 3회 실행 합산 **0.75812** (+0.0084).
추가 GPU 비용 없이 얻은 이득이다.

이를 정확히 하기 위해 추론 스크립트가 **표 분포 전체**(`{답: 표수}`)를 저장하도록 했다.
합의율만으로 근사하면 2등 이하 표가 사라져 합산이 틀어진다.

### 5-5. 표 수(n) 배분

같은 생성 결과에서 앞 k개만 써서 투표하면 n별 정확도 곡선을 추가 비용 없이 얻을 수 있다.
RFT 생성(18,319문항) 중 측정한 값:

| n | 정확도 | 증분 |
|---|---|---|
| 1 | 0.5488 | |
| 2 | 0.5968 | +0.0480 |
| 4 | 0.6569 | +0.0601 |
| 8 | 0.6978 | +0.0409 |

n=8에서도 증분이 +0.04로, 아직 포화하지 않았다. 최종 추론에서는 시간이 허락하는 최대 n을 쓴다.

**2단계 적응형 배분**: 전체를 n=32로 1패스 → 합의율 < 0.4인 문항(약 32%)만 골라
높은 n으로 재실행 → 표 합산. 같은 GPU 시간을 분산이 집중된 구간에 몰아준다.

### 5-6. 최종 추론 실행 조건

- 캐글 노트북, **인터넷 OFF** (규칙 6a)
- T4 × 2 데이터 병렬 (`--shard i --num-shards 2`, GPU당 독립 프로세스)
  `tensor_parallel_size=2`는 `/dev/shm` 문제로 실패하므로 쓰지 않는다
- 청크 단위 백업으로 세션 분할 실행 지원 (캐글 세션 한도 12시간)

---

## 6. 재현 시 유의사항 (규칙 8.2b)

**RFT 데이터는 확률적 생성물이다.** `seed=1234`를 고정했으나 vLLM 버전이나 GPU 종류가
다르면 완전히 동일한 풀이가 나오지 않는다. 따라서 **생성된 `rft_data.csv`를 산출물로 함께
제출**한다. 코드 재실행이 아니라 데이터 자체를 넘겨 재현 가능성을 확보한다.

파이프라인 실행 순서:

```
01_clean_data.py       원본 정제 → clean_v4 + local_val_500
04_add_external_data.py 외부 데이터 추가 → clean_v5
07_make_rft_pool.py    RFT 대상 풀 (유출 제거) → rft_pool.csv
02_infer_vllm.py       --dump-gens 로 풀이 생성 (GPU)
10_find_bad_labels.py  라벨 불량 목록 (제보 + 자동탐지) → bad_label_ids.csv
06_build_rft_data.py   정답 대조·필터·라벨불량 제외 → rft_data.csv + unsolved_ids.csv
09_build_train_mix.py  최종 학습셋 → train_mix_v6.csv
03_train_sft.py        QLoRA SFT → 16bit 병합 (GPU)
02_infer_vllm.py       최종 추론 (GPU, 인터넷 OFF)
05_check_submission.py 제출 형식 검사
```

보조 도구: `08_diag_seen_unseen.py` (학습 포함 여부별 정확도 진단 — §2-3의 편향을 찾아낸 도구)

---

## 7. 사용한 외부 데이터셋 (규칙 5.2c)

모두 무료로 공개되어 모든 참가자가 동등하게 접근 가능하다.

| 데이터셋 | 출처 | 라이선스 | 사용량 |
|---|---|---|---|
| OpenMathInstruct-2 | `nvidia/OpenMathInstruct-2` (HuggingFace) | CC-BY-4.0 | 25,000 수집 |
| NuminaMath-CoT | `AI-MO/NuminaMath-CoT` (HuggingFace) | Apache-2.0 | 7,000 수집 |
| GSM8K | `openai/gsm8k` (HuggingFace) | MIT | 2,350 (전량 사용) |

앞의 두 출처는 정제 단계에서 `external` 단일 풀(32,000문항)로 합쳐졌고,
최종 학습에는 그중 **12,000문항을 무작위 추출**해 사용했다.
합친 뒤 표집했으므로 출처별 최종 사용량은 개별로 집계되지 않는다.
external을 전량이 아니라 12,000으로 줄인 이유는 §2의 분포 불일치 때문이다 —
다양성 확보용 보조 데이터이지 주된 학습 신호가 아니다.

주최측 제공 데이터 외에 상용 API로 생성한 데이터는 사용하지 않았다.
test 문항의 답을 외부에서 구하거나 생성한 사실이 없다 (규칙 5.3b, 10.1a).

---

## 8. 재현 환경 (규칙 8.2a)

| 항목 | 값 |
|---|---|
| 학습 하드웨어 | Kaggle Notebook, NVIDIA Tesla T4 (16GB) × 1 사용 |
| 추론 하드웨어 | Tesla T4 × 1~2 |
| Python | 3.12 (Kaggle) / 3.13 (Colab) |
| 학습 라이브러리 | unsloth 2026.8.19, TRL, transformers, PyTorch |
| 추론 라이브러리 | vLLM 0.27.1 (Kaggle) / 0.28.0 (Colab), 둘 다 `dtype=half` |
| 학습 소요 | **7시간 57분** — T4 1장, 2,579 스텝, 41,274샘플 / 13.5M 토큰, 1 epoch |

**주의**: T4(compute capability 7.5)는 bfloat16과 FlashAttention-2를 지원하지 않는다.
vLLM은 `dtype=half` + TRITON_ATTN 백엔드로 동작한다.
vLLM과 unsloth를 같은 환경에 설치하면 PyTorch 빌드가 충돌하므로 **학습과 추론 환경을 분리**해야 한다.

또한 unsloth는 무료판이 멀티 GPU 학습을 지원하지 않는데, GPU가 2장 **보이기만 해도**
레이어를 두 장에 나눠 올린 뒤 임베딩 조회에서 죽는다. CUDA 초기화 전에
`CUDA_VISIBLE_DEVICES=0` 을 고정해야 한다.

vLLM 0.28은 엔진 워밍업 경로가 `torchvision` 을 무조건 import 한다(텍스트 전용 모델이어도
마찬가지다). 오프라인 설치용 휠 묶음에 `torchvision` 이 빠져 있으면 모델 로딩과 KV 캐시
할당이 모두 끝난 **마지막 단계에서** `ModuleNotFoundError` 로 죽으므로,
인터넷을 끈 최종 제출 환경에서는 특히 주의해야 한다.

---

## 9. 결과

| 구성 | 로컬 검증 (`local_val_500`) | 리더보드(Public) |
|---|---|---|
| 순정 Qwen2.5-3B-Instruct | 0.5380 | — |
| v4 (1차 정제 + 외부 40k, n=16) | 0.7280 | — |
| v4 (n=32) | — | 0.74969 |
| v4 (n=32 × 3회 합산) | — | **0.75812** |
| v5 (RFT 재학습) 「TBD」 | 「TBD」 | 「TBD」 |
| 최종 제출 (2단계 적응형 + 합산) | — | 「TBD」 |

---

## 10. 한계와 남은 개선 여지

- **어려운 문항이 천장이다.** 합의율 0.4 미만 구간(전체의 32%)은 정확도 32.7%이며,
  표를 늘려도 분포 자체가 틀린 경우가 많다. 실제로 오답을 열어보면 수열·조합론·올림피아드
  수준의 문항으로, 추출 버그가 아니라 3B 모델의 능력 한계다.
- **유형별 편차**: 서술형 0.798 대 LaTeX 0.648, 질문 300자 이상 장문 0.541.
- **RL 미적용**: 규칙 4.2가 GRPO 등을 허용하고 정수 정답은 검증 가능한 보상으로 이상적이지만,
  T4는 bf16을 지원하지 않아 fp16 GRPO의 불안정성 위험이 크고, 사용 가능한 GPU 예산 안에서
  롤아웃 생성 비용을 감당할 수 없었다. RFT를 그 대체로 선택했다 —
  on-policy 정답 신호를 쓰되 학습 불안정성이 없고, 생성이 문항 단위로 독립이라
  세션이 자주 끊기는 무료 환경에 적합하다.
- **미해결 3,378문항**: 8회 시도에서 한 번도 정답에 도달하지 못한 문항.
  이 중 상당수가 `max_tokens` 에 잘려 있었으므로, 토큰 한도를 1024에서 2048로 올리면
  "풀 수는 있는데 지면이 모자랐던" 문항이 회수될 것으로 예상하고 2차 생성을 시도했다.
  **결과는 정확도 0.0583으로, 회수량은 무시할 수준이었다.**
  주목할 점은 한도를 두 배로 늘렸는데도 잘림률이 여전히 22.5%였다는 것이다.
  인과가 반대였다 — 잘려서 못 푼 게 아니라, 못 푸는 문항이라 끝없이 헤매다 잘린 것이다.
  길이는 증상이지 원인이 아니었다. 이 문항들은 학습 데이터에서 제외하는 것이 맞다.
